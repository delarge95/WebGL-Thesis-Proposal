\section{Desarrollo e Implementación}

Este capítulo documenta la versión funcional visible del prototipo y separa con claridad lo expuesto en la UI final de lo que permanece oculto, legado o proyectado. La escena oficial de referencia es \texttt{MainScene\_Final}.

\subsection{Normalización editorial del sistema}

Para evitar contradicciones entre código, escena y documentación, este informe adopta las siguientes convenciones:

\begin{itemize}
    \item \textbf{28 piezas canónicas} de investigación definidas en \texttt{x500v2\_parts\_data.json};
    \item \textbf{30 anchors de escena}: las 28 piezas más \texttt{x500v2\_fastener\_group} y \texttt{x500v2\_misc\_group};
    \item \textbf{257 renderers/colliders} auditados en la escena final;
    \item \textbf{257 assets generados} en \texttt{Assets/Core/Data/X500V2Generated};
    \item inventario de scripts expresado con tres niveles: 95 runtime propios, 103 bajo \texttt{Assets} excluyendo tests, y 129 archivos \texttt{.cs} totales incluyendo editor/plugins.
\end{itemize}

La cifra histórica de ``16 piezas'' se conserva solo como antecedente de etapas previas del proyecto y no como conteo final del sistema documentado.

\subsection{Arquitectura operativa}

La arquitectura final puede leerse en cuatro capas:

\begin{enumerate}
    \item \textbf{Presentación.} UI Toolkit mediante \texttt{MainLayout.uxml}, USS y controladores de interfaz.
    \item \textbf{Orquestación.} \texttt{UIManager}, \texttt{AppStateMachine} y \texttt{UIModeController}.
    \item \textbf{Servicios de escena.} selección, explode, clipping, visibilidad, view modes, entorno y subsistema térmico.
    \item \textbf{Datos.} \texttt{DronePartData}, assets generados, catálogo canónico JSON y catálogos modulares de fasteners.
\end{enumerate}

Las Figuras \ref{fig:arquitectura_capas_sistema}, \ref{fig:flujo_eventbus_sistema} y \ref{fig:app_state_machine_sistema} sintetizan la arquitectura por capas, la comunicación desacoplada por eventos y la máquina de estados que organiza el flujo principal de la aplicación.

\begin{figure}[H]
    \caption{Arquitectura por capas del sistema.}
    \label{fig:arquitectura_capas_sistema}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_1.pdf}
\end{figure}

\begin{figure}[H]
    \caption{Flujo de comunicación EventBus bajo el patrón \textit{publish/subscribe}.}
    \label{fig:flujo_eventbus_sistema}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_2.pdf}
\end{figure}

\begin{figure}[H]
    \caption{Máquina de estados de la aplicación (\texttt{AppStateMachine}).}
    \label{fig:app_state_machine_sistema}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_3.pdf}
\end{figure}

\subsection{Bootstrap y saneamiento del import}

La escena final depende de dos mecanismos complementarios.

\paragraph{Garantía de managers.} Durante la inicialización, \texttt{UIManager} asegura la presencia de los managers mínimos requeridos por el visor: selección, explode, catalogación de runtime, notificaciones, hotspots, clipping, visibilidad, estado del dron y subsistema térmico.

\paragraph{Reparación del modelo importado.} \texttt{ImportedDroneRuntimeBinder} corrige la jerarquía del dron importado en runtime. Su trabajo incluye:
\begin{itemize}
    \item resolver anchors por \texttt{id};
    \item reubicar objetos huérfanos de nivel superior o anidados;
    \item sintetizar grupos para fasteners y misceláneos;
    \item reconstruir cachés de \texttt{ExplodedViewManager}, \texttt{PartVisibilityManager}, \texttt{PartCatalogManager}, \texttt{CrossSectionManager}, \texttt{ViewModeManager} y el subsistema térmico.
\end{itemize}

Este saneamiento permite que la versión importada del modelo mantenga coherencia funcional con la taxonomía académica del proyecto.

Sobre esa misma base se implementó una capa Unity específica para fasteners. Dicha capa introduce los catálogos \texttt{holybro\_fastener\_families.json}, \texttt{holybro\_fastener\_instances.json} y \texttt{holybro\_fastener\_reconciliation.json}, un registro runtime para resolver \texttt{instanceId -> family -> recipe} y un flujo de inspección bajo demanda que conserva proxies ligeros en reposo. En la escena documentada quedaron versionadas \textbf{20 familias}, \textbf{168 instancias} y \textbf{9} entradas de reconciliación. Debe aclararse que la geometría visible actual sigue siendo temporal; el aporte implementado en Unity fija el contrato de datos y la lógica de inspección para que las mallas Blender finales puedan reemplazar los placeholders sin reescribir el runtime.

La Figura \ref{fig:flujo_dronepartdata_runtime} resume el tránsito de \texttt{DronePartData} desde el asset o catálogo hasta los controladores de interfaz y los servicios de escena, lo cual explica por qué la reparación de runtime es determinante para la consistencia del producto final.

\begin{figure}[H]
    \caption{Flujo de datos \texttt{DronePartData} desde asset y catálogo hasta la interfaz.}
    \label{fig:flujo_dronepartdata_runtime}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_6.pdf}
\end{figure}

\subsection{Flujo visible de la build final}

El flujo principal de uso es:
\begin{center}
\texttt{Hero -> Explore -> selección -> bottom sheet -> Inspect / Analyze / Studio}
\end{center}

\paragraph{Hero.} El Hero funciona como puerta de entrada al prototipo. En la versión saneada para cierre, sus textos se anclan al caso real del Holybro X500~V2 y el acceso al repositorio queda cableado desde el submenú \texttt{About}.

\paragraph{Selección y detalle.} Al seleccionar una pieza, el sistema resalta el objeto y abre una ficha inferior (\textit{bottom sheet}) con información de identificación, especificaciones y ensamblaje. Cuando la selección pertenece a la categoría \texttt{Fasteners}, la ficha usa \texttt{FastenerMetadata} para mostrar familia, métrica, longitud, material, fuente CAD y anchor canónico inferido.

\paragraph{Inspect.} Reúne hotspots, aislamiento de la pieza seleccionada y control de energía/carga del dron.

\paragraph{Analyze.} Integra corte transversal, explode y filtros por categorías funcionales. La taxonomía activa en esta vista incluye \texttt{SkeletonAirframe}, \texttt{PropulsionSystem}, \texttt{Avionics}, \texttt{SensorsComms}, \texttt{PowerDistribution} y \texttt{Fasteners}.

\paragraph{Studio.} Agrupa modos visuales y control de entorno. El estado base es \texttt{Realistic}; la UI visible expone \texttt{X-Ray}, \texttt{Solid Color} y \texttt{Thermal}, junto con tarjetas de ambiente y sliders de luz.

La Figura \ref{fig:flujo_seleccion_ui} muestra el recorrido de selección desde el input del usuario hasta la sincronización con el \textit{bottom sheet}, mientras que la Figura \ref{fig:pipeline_shaders_modos} resume la relación entre los modos visuales implementados y los shaders o materiales que los soportan.

\begin{figure}[H]
    \caption{Flujo de selección de piezas desde \textit{input} y \textit{raycast} hasta la interfaz.}
    \label{fig:flujo_seleccion_ui}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_4.pdf}
\end{figure}

\begin{figure}[H]
    \caption{Pipeline de shaders por modo de visualización, distinguiendo entre modos implementados y modos expuestos en la UI final.}
    \label{fig:pipeline_shaders_modos}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_5.pdf}
\end{figure}

\subsection{Matriz de estado funcional}

\subsubsection{Expuesto en UI final}

\begin{itemize}
    \item Hero y navegación principal;
    \item selección de piezas y \textit{bottom sheet};
    \item hotspots;
    \item isolate;
    \item power y slider de carga;
    \item explode;
    \item cross-section;
    \item filtros por categoría;
    \item thermal con leyenda visible.
\end{itemize}

\subsubsection{Implementado pero oculto}

\begin{itemize}
    \item \texttt{MeasurementTool};
    \item modos de vista \texttt{Blueprint}, \texttt{Wireframe} y \texttt{Ghosted} como capacidades implementadas pero no expuestas en la UI final;
    \item parte del flujo térmico avanzado y del clipping extendido.
\end{itemize}

\subsubsection{Código legado o no integrado}

\begin{itemize}
    \item \texttt{PartCatalogUI};
    \item \texttt{SettingsPanel};
    \item \texttt{LoadingController};
    \item partes de \texttt{EnhancedInfoPanel};
    \item suite de ensamblaje, BOM, anotaciones y connections.
\end{itemize}

\begin{table}[H]
    \caption{Inventario sintético de módulos y estado de integración}
    \label{tab:inventario_modulos_integracion}
    \centering
    \small
    \begin{tabular}{p{3.2cm}p{4.3cm}p{3.0cm}p{4.0cm}}
        \hline
        \textbf{Categoría} & \textbf{Componentes representativos} & \textbf{Estado} & \textbf{Lectura editorial} \\ \hline
        Runtime principal & Orquestación UI, selección, explode, clipping, view modes y térmico & Expuesto o activo en la build final & Debe documentarse en el cuerpo principal porque soporta el flujo visible y la arquitectura operativa. \\
        Capacidades ocultas & \texttt{MeasurementTool}; modos \texttt{Blueprint}, \texttt{Wireframe} y \texttt{Ghosted}; clipping extendido; térmico avanzado & Implementado pero oculto & Puede mencionarse como capacidad existente, pero no debe narrarse como funcionalidad publicada al usuario final. \\
        Legado o no integrado & Catálogo legado, \texttt{SettingsPanel}, \texttt{LoadingController}, paneles legacy y suite de ensamblaje & No integrado en el flujo final & Debe permanecer separado como legado, trabajo futuro o evidencia histórica de evolución del proyecto. \\
        Tooling de editor & Wizard de proyecto, auditoría de cobertura y constructor del grafo térmico & Soporte técnico de desarrollo & Es pertinente para la defensa técnica y la trazabilidad del pipeline, aunque no forme parte de la UI publicada. \\ \hline
    \end{tabular}
    \par\vspace{0.5\baselineskip}
    \textit{Nota}. Esta tabla resume el criterio usado para separar funciones visibles, funciones ocultas y módulos no integrados, evitando mezclar capacidades de desarrollo con el alcance real de la build final. \textit{Fuente}. Elaboración propia a partir del código y de la validación funcional del proyecto.
\end{table}

\subsection{Sistema de visualización y shaders}

\texttt{ViewModeManager} implementa siete modos de vista: \texttt{Realistic}, \texttt{XRay}, \texttt{Blueprint}, \texttt{SolidColor}, \texttt{Wireframe}, \texttt{Ghosted} y \texttt{Thermal}. La UI visible no expone todos los modos al mismo tiempo; por ello el informe diferencia entre capacidad implementada y capacidad publicada en la build final.

En particular, el modo \texttt{Thermal} se apoya en \texttt{ThermalSimulationManager} y \texttt{ThermalViewController}, y utiliza una leyenda visual en interfaz para mantener lectura clara del gradiente de temperatura.

\subsection{Modelo matemático operativo del subsistema térmico}

El modo \texttt{Thermal} no implementa una simulación FEA ni una solución termodinámica de alta fidelidad. La versión final usa un solver reducido por componentes (\textit{lumped-component model}) coherente con la arquitectura real del código: \texttt{DroneStateController} aporta el factor de carga, \texttt{ThermalSimulationManager} resuelve temperaturas por nodo y \texttt{ThermalViewController} traduce esas temperaturas a bandas visuales para shader y leyenda.

Para cada nodo térmico, la temperatura de equilibrio depende de la carga efectiva del dron. Si la pieza no es fuente de calor, se toma:
\[
T_{eq} = T_{amb}
\]

Para fuentes térmicas, el equilibrio se interpola entre temperatura ambiente, temperatura de hover y temperatura pico según el factor de carga efectivo del sistema. La aproximación temporal hacia ese equilibrio se modela como:
\[
\Delta T_{\text{source}} = \left(T_{eq} - T_{actual}\right)\left(1 - e^{-\Delta t / \tau}\right)w_s
\]
donde $\tau$ es el tiempo de calentamiento configurado para la pieza y $w_s$ es su peso relativo como fuente térmica.

El enfriamiento ambiental se aplica como:
\[
\Delta T_{\text{cooling}} = \left(T_{amb} - T_{actual}\right)c_r e \Delta t
\]
donde $c_r$ es la tasa de enfriamiento y $e$ el factor de exposición convectiva.

La transferencia entre piezas conectadas por el grafo térmico se aproxima mediante:
\[
\Delta T_{ij} = \left(T_j - T_i\right)\hat{G}_{ij}\Delta t
\]
con un coeficiente de acoplamiento geométrico definido como:
\[
\hat{G}_{ij} = \max\left(0.001,\; s_{ij}\frac{A_{ij}}{L_{ij}}\right)
\]
donde $A_{ij}$ corresponde al área de contacto proxy, $L_{ij}$ a la longitud efectiva del camino térmico y $s_{ij}$ a un factor heurístico de enlace. En consecuencia, el sistema comparte la lógica de una conducción tipo Fourier, pero no debe interpretarse como una conductancia física calibrada en unidades SI.

La trazabilidad matemática del subsistema fue contrastada con el workflow de verificación basado en WolframAlpha utilizado por el proyecto. Dicho audit confirmó tres puntos: la estructura geométrica inspirada en $A/L$ es razonable para el acoplamiento entre nodos, los tiempos de calentamiento del solver están deliberadamente comprimidos frente a valores físicos reales para mantener legibilidad interactiva, y la tasa de enfriamiento por defecto es coherente con esa misma escala temporal acelerada. Por ello, este subsistema debe documentarse como simulación térmica híbrida y heurística, no como FEA en tiempo real.

La Figura \ref{fig:subsistema_termico_hibrido} articula la relación entre el estado del dron, la simulación térmica y la traducción visual del gradiente en la build final.

\begin{figure}[H]
    \caption{Arquitectura del subsistema térmico híbrido.}
    \label{fig:subsistema_termico_hibrido}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_7.pdf}
\end{figure}

\subsection{Tooling de editor y verificación}

El proyecto conserva herramientas de editor que son defendibles como parte del cierre técnico:
\begin{itemize}
    \item \texttt{ProjectSetupWizard};
    \item \texttt{ImportedDroneCoverageAudit};
    \item \texttt{ThermalContactGraphBuilderWindow};
    \item fixers de configuración y build WebGL.
\end{itemize}

Estas herramientas ayudan a mantener consistencia entre escena, catálogo, render y configuración de despliegue.

Las Figuras \ref{fig:restricciones_webgl_mitigaciones} y \ref{fig:despliegue_artefactos_build} complementan esta lectura al mostrar, respectivamente, las restricciones técnicas del destino WebGL y el flujo de artefactos desde la build hasta la validación del despliegue.

\begin{figure}[H]
    \caption{Restricciones de WebGL y mitigaciones técnicas aplicadas al proyecto.}
    \label{fig:restricciones_webgl_mitigaciones}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_8.pdf}
\end{figure}

\begin{figure}[H]
    \caption{Flujo de despliegue y artefactos desde la build hasta el \textit{hosting} y QA.}
    \label{fig:despliegue_artefactos_build}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_10.pdf}
\end{figure}

\subsection{Limitaciones activas al cierre}

Al momento de esta versión documental, los siguientes frentes siguen abiertos y afectan el cierre total del producto:
\begin{enumerate}
    \item aplicar la optimización final del pipeline CAD $\rightarrow$ Unity según los planes de Holybro;
    \item rerun de auditorías jerárquicas y de cobertura tras el reimport final;
    \item decidir si el audio entra antes del freeze o queda como trabajo futuro;
    \item congelar la build definitiva para ejecutar pruebas de usabilidad y reportes finales.
\end{enumerate}

Mientras estas tareas no concluyan, el sistema se documenta como funcional y verificable en su flujo principal, pero no como versión empíricamente cerrada.

La Figura \ref{fig:herramientas_ensamblaje_proyectadas} deja explícito que el conjunto de herramientas de ensamblaje, BOM, anotaciones y conexiones pertenece a la capa proyectada o no integrada, por lo que no debe confundirse con el flujo visible de la build final.

\begin{figure}[H]
    \caption{Estado de herramientas de ensamblaje proyectadas o no integradas por módulo.}
    \label{fig:herramientas_ensamblaje_proyectadas}
    \centering
    \includegraphics[width=\textwidth,height=0.78\textheight,keepaspectratio]{figures/chapter4/fig_4_9.pdf}
\end{figure}

\subsection{Placeholders editoriales de capturas y renders}

Con el fin de dejar cerrada la maqueta editorial del capítulo 4 y preparar la captura final del producto, este informe reserva las Figuras \ref{fig:placeholder_hero_holybro} a \ref{fig:placeholder_retopologia}. Estas reservas no sustituyen evidencia; solo fijan ubicación, caption, intención narrativa y estado funcional esperado de cada captura.

\placeholderfigure
    {Pantalla Hero del visor Holybro X500~V2.}
    {fig:placeholder_hero_holybro}
    {Inserte aquí la captura de apertura del visor con el caso Holybro X500~V2, CTA principal y copy saneado.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Vista principal del dron en modo \texttt{Realistic}.}
    {fig:placeholder_realistic_view}
    {Inserte aquí una captura del estado base del visor con materialidad PBR, iluminación neutra y modelo completo.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Vista explosionada del ensamblaje.}
    {fig:placeholder_exploded_view}
    {Inserte aquí la captura del modo \textit{explode} mostrando separación legible de piezas y lectura de ensamblaje.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Modo \texttt{X-Ray} para inspección interna.}
    {fig:placeholder_xray_view}
    {Inserte aquí la captura del modo \texttt{X-Ray} con lectura clara de estructura interna y pieza seleccionada.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Modo \texttt{Wireframe} para lectura de malla.}
    {fig:placeholder_wireframe_view}
    {Inserte aquí una captura de referencia del modo \texttt{Wireframe}.}
    {Estado esperado: capacidad implementada pero oculta; si no se publica, debe migrarse a anexos o trabajo futuro.}

\placeholderfigure
    {Modo \texttt{Thermal} con leyenda visible.}
    {fig:placeholder_thermal_view}
    {Inserte aquí la captura del gradiente térmico con componentes críticos y leyenda sincronizada.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Corte transversal con plano activo.}
    {fig:placeholder_cross_section}
    {Inserte aquí la captura del modo de corte mostrando el plano activo y la lectura del interior del dron.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Controles de filtros por categoría y exploración de piezas.}
    {fig:placeholder_filters_categories}
    {Inserte aquí la captura de los filtros funcionales disponibles en \texttt{Analyze} y su efecto sobre la escena.}
    {Estado esperado: flujo visible de la build final; no debe confundirse con un catálogo legado no expuesto.}

\placeholderfigure
    {Ficha inferior de detalle (\textit{bottom sheet}) de una pieza.}
    {fig:placeholder_bottom_sheet}
    {Inserte aquí la captura de una pieza seleccionada con nombre, especificaciones y ensamblaje en la ficha inferior.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Hotspots sobre el modelo con estado de selección.}
    {fig:placeholder_hotspots}
    {Inserte aquí la captura de hotspots activos sobre el dron y su correspondencia con el modelo.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Comparativa de presets de entorno en \texttt{Studio}.}
    {fig:placeholder_studio_environment}
    {Inserte aquí una comparación entre dos presets de entorno o iluminación del modo \texttt{Studio}.}
    {Estado esperado: flujo visible de la build final.}

\placeholderfigure
    {Vista responsive del prototipo en pantalla móvil.}
    {fig:placeholder_mobile_responsive}
    {Inserte aquí la captura del layout adaptado a móvil, con foco en legibilidad y uso táctil.}
    {Estado esperado: evidencia de despliegue WebGL y adaptación visual post-freeze.}

\placeholderfigure
    {Comparativa de retopología entre malla de origen y versión optimizada.}
    {fig:placeholder_retopologia}
    {Inserte aquí una comparativa visual entre high-poly/entrada CAD y modelo optimizado para WebGL.}
    {Estado esperado: evidencia técnica del pipeline CAD a Unity documentado en el proyecto.}

\newpage
